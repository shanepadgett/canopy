# Task 03 - Pagination

## Goal

Paginate list pages (home, section indexes, tag indexes) when item counts grow.

## Scope

- Configurable page size with defaults.
- Generate `/page/2/` style URLs.
- Provide pagination data to templates.

## Notes

- Decide whether to use a shared pagination struct in template data.
