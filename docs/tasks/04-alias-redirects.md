# Task 04 - Alias Redirects

## Goal

Generate redirect files for `Page.Aliases` to preserve old URLs.

## Scope

- For each alias URL, emit an HTML redirect page.
- Ensure sitemap and RSS point at canonical URLs only.

## Notes

- Decide on redirect HTML template and meta refresh.
